apiVersion: v1
kind: Service
metadata:
  name: grafana-demo
spec:
  selector:
    app: grafana-demo
  ports:
    - port: 3000
      targetPort: 3000

---

apiVersion: v1
kind: Service
metadata:
  name: prometehus-demo
spec:
  selector:
    app: prometheus-demo
  ports:
    - port: 9090
      targetPort: 9090
